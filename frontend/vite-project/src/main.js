import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
import router from './router' // Make sure to import Vue Router
createApp(App).use(router).mount('#app')

//createApp(App).mount('#app')
